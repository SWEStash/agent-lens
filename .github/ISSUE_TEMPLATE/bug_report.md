---
name: Bug report
about: Something doesn't work as expected
labels: bug
---

**What happened, and what did you expect instead?**


**Steps to reproduce**

1.
2.

**Which command?** (e.g. `agent-lens collect`, `ingest`, `serve`, `watch`, `export`)


**Environment**

- `agent-lens --version`:
- OS:
- Node version (`node --version`, needs ≥ 24):

**Diagnostics** — output of `agent-lens config`, or the `/about` page in the UI. Paste the
versions and paths sections; redact anything you'd rather not share.

```

```

**Please don't paste raw session transcripts** — they're your real agent sessions and can contain
secrets. If a specific session is needed, `agent-lens export <sessionId>` redacts by default.
